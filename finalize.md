# ZeroShell — Final Review & Task List

تاریخ بازبینی: 2026-09-06 — وضعیت: **قابل اجرا به‌عنوان نمونه اولیه؛ هنوز آماده انتشار پایدار و پاک‌سازی خودکار سایت واقعی نیست.**

این سند فقط نتیجه بررسی و کارهای باقی‌مانده است. در این بازبینی کد اجرایی، تست‌های پروژه، README و پلن قبلی تغییر نکردند. تمام آزمون‌های تغییردهنده روی کپی موقت و فایل‌های ساختگی انجام شدند.

- Commit بررسی‌شده: `ac1cb90` — `Add ZeroShell drop-in WordPress malware cleaner.`
- SHA-256 فایل فعلی `malware-cleaner.php`: `4f42bf23bd9829d8731b5cffd97e60ede48e688c3574ee18d9789dd20bd5cb3a`
- محیط اجرای بررسی: PHP 8.5.8 روی macOS؛ وب‌سرور آزمایشی PHP روی localhost.
- نمره فعلی: **۱۰ از ۲۰**؛ ارزیابی مهندسی آمادگی استفاده، نه درصد دقت تشخیص بدافزار.

## 1. پاسخ به هدف اصلی

**بله، معماری می‌تواند به هدف برسد و بخش تک‌فایلی همین حالا عملاً کار می‌کند.** خروجی بدون `src/`، `rules/`، Composer یا Phar در یک سایت موقت اجرا شد، فایل‌های مشکوک را پیدا کرد و پیش‌نمایش دستی و قرنطینه/بازیابی ساده کار کردند.

اما سه مرز باید روشن بمانند:

1. «تک‌فایل» یعنی فقط یک فایل برای نصب؛ تنظیمات، صف، کش و نسخه‌های قرنطینه در زمان اجرا به فضای قابل نوشتن نیاز دارند. نوشتن این اطلاعات داخل خود اسکریپت پیشنهاد نمی‌شود.
2. «اسکن کل سایت» فعلاً به معنی پیمایش پوشه‌ها و بررسی فایل‌های دارای `php`، نام شامل `.php`، `suspected` و `bak` است. `.phtml`، `.php7`، JavaScript، HTML، `.htaccess`، `.user.ini` و دیتابیس تحت پوشش کامل نیستند. AI فقط فایل‌های رسیده به review را می‌بیند؛ فایل ازدست‌رفته در مرحله تشخیص به AI نمی‌رسد.
3. هیچ رأی AI یا امضای ساده‌ای تضمین نمی‌کند یک فایل قطعاً سالم یا قطعاً مخرب است. خروجی باید «مشکوک»، «مخرب بر اساس شواهد»، «تأیید سالم توسط کاربر» و «نامطمئن» را جدا کند. امتیاز confidence مدل، احتمال کالیبره‌شده خطا نیست.

پس از رفع تسک‌های P0 و P1 می‌توان نسخه‌ای قابل اتکا برای **یافتن و بازبینی فایل‌های مشکوک** منتشر کرد؛ ادعای تشخیص همه آلودگی‌ها یا پاک‌شدن کامل سایت قابل تأیید نیست.

## 2. شواهد اجرا و حدود بررسی

| بررسی | نتیجه واقعی |
|---|---|
| تست‌های موجود | **22 passed / 0 failed** روی PHP 8.5.8 |
| Build در کپی موقت | موفق؛ هر دو خروجی root و `dist/` دقیقاً با فایل‌های تحویلی یکسان بودند |
| نصب فقط یک فایل PHP | موفق؛ بدون source tree و قواعد خارجی |
| اسکن CLI و Web روی نمونه کوچک | هرکدام ۴ فایل واجد شرایط، ۳ finding؛ فایل‌های `.phtml` و `.js` با همان نشانگر بررسی نشدند |
| Preview دستی | موفق |
| دو بار Mark legitimate برای دو مسیر | `candidate_added` سپس `promoted_to_trusted` |
| حذف و Restore ساده روی نمونه ساختگی | موفق |
| یک پوشه با ۶۲۰ فایل PHP | **همه ۶۲۰ فایل در یک batch** پردازش شدند؛ سقف ۵۰۰ واقعی نیست |
| فایل تغییرکرده بعد از scan + رأی مخرب کش‌شده قدیمی | **فایل جدید با cache hit و checksum قدیمی قرنطینه شد** |
| دسترسی بی‌احراز هویت به state روی سرور بدون اجرای `.htaccess` | HTTP **200** و JSON قابل خواندن |
| درخواست HTTP به نسخه قرنطینه‌شده یک PHP بی‌خطر | کد `echo` آن **اجرا شد**؛ فایل صرفاً بایگانی غیرقابل‌اجرا نیست |
| Restore روی فایل موجود | نسخه فعلی بدون محافظت overwrite شد |
| Restore از مسیر دارای parent symlink | فایل **بیرون root** نوشته شد |
| پیمایش directory symlink به بیرون root | فایل خارجی وارد یافته‌های scan شد |
| Normalized hash برای دو محتوای متفاوت | دو فایل با بایت و رفتار متفاوت یک normalized hash گرفتند |
| پاسخ AI با confidence نامعتبر `90invalid` | به عدد `90` تبدیل و شرط حذف را پاس کرد |
| هم‌زمانی state | ذخیره snapshot قدیمی وضعیت جدید `AI_QUARANTINED` را به `FOUND` برگرداند |
| اطلاعات حساس | کلید ساختگی AI در HTML تنظیمات برگشت؛ ذخیره تنظیم غیرمرتبط، کلید محیطی ساختگی را هم روی دیسک نوشت |
| TRUSTED_HIDDEN در report | هر ۳ رکورد trusted همچنان داخل `window.REVIEW_ITEMS` بودند |
| ورود مجدد عادی با POST و کلید صحیح | HTTP **400** با `Unknown action.`؛ cookie ورود ساخته نشد |
| ورود با GET key | کلید از URL حذف نشد و cookie احراز هویت ساخته نشد |
| داده مسیر در JavaScript گزارش | رشته بستن `script` از مسیر فایل، بدون escaping مناسب داخل HTML قرار گرفت؛ JavaScript آزمایشی اجرا نشد |

محدودیت‌های این بررسی:

- تماس واقعی با Gemini و ارسال به توسعه‌دهنده انجام نشد؛ تست AI با پاسخ ساختگی یا کش آماده بود. کلید واقعی برای آزمون مصرف نشد.
- PHP 7.4، Apache و Nginx واقعی اجرا نشدند؛ سازگاری آن‌ها باید در تسک تست انتشار تأیید شود. آزمون سرور PHP صرفاً نشان می‌دهد محافظت فعلی به اجرای `.htaccess` وابسته است.
- بررسی بصری مرورگر به صفحه ورود محدود شد؛ آزمون‌های جریان اصلی با HTTP محلی و کلاس‌های واقعی انجام شدند. ارزیابی کامل موبایل، RTL و دسترس‌پذیری هنوز لازم است.
- مجموعه واقعی و برچسب‌خورده بدافزار/فایل سالم در اختیار نبود؛ نرخ false positive و false negative اندازه‌گیری نشده است.
- تست ۸ فعلی شرط تصمیم را در خود تست دوباره می‌نویسد و endpoint واقعی را اجرا نمی‌کند. تست ۱۰ نیز دو claim پشت‌سرهم در یک process است، نه آزمون واقعی هم‌زمانی.

## 3. شیوه اجرای تسک‌ها

- **P0:** مانع انتشار و استفاده از حذف خودکار روی سایت واقعی.
- **P1:** لازم برای تکمیل رفتار وعده‌داده‌شده و انتشار پایدار.
- **P2:** تکمیل تجربه کاربری و مستندات؛ پس از P0/P1.
- فقط `src/` و منابع مربوط را اصلاح کن؛ خروجی‌های `malware-cleaner.php` و `dist/malware-cleaner.php` را با build بازتولید کن.
- هر تسک باید آزمونی داشته باشد که قبل از اصلاح نقص را نشان دهد و بعد از اصلاح موفق شود. لازم نیست framework جدید اضافه شود.
- شماره خطوط زیر مربوط به commit بررسی‌شده‌اند؛ نام متدها مرجع پایدارتر است.
- در موارد مشخص‌شده، خود تصمیم پلن قبلی هم نیازمند اصلاح است؛ آن را برای حفظ رفتار آسیب‌پذیر معیار قرار نده.

## 4. P0 — اصلاحات ضروری پیش از استفاده واقعی

### [ ] F01 — قرنطینه و اطلاعات runtime را مستقل از وب‌سرور محافظت کن

**محل:** `src/Config.php:9,25,49`، `src/Quarantine.php:24`، `src/Store.php`.

**مشکل:** پیش‌فرض داده‌ها داخل web root است. `.htaccess` و `index.php` از درخواست مستقیم فایل در همه سرورها جلوگیری نمی‌کنند. نسخه قرنطینه پسوند `.php` و محتوای اجرایی اصلی را نگه می‌دارد؛ manifest نیز مسیر آن را افشا می‌کند. حتی انتخاب data directory بیرون root، در سایت وردپرسی قرنطینه را همچنان زیر `wp-content` قرار می‌دهد.

**کار لازم:**

- قرنطینه را زیر data directory امن قرار بده؛ مسیر موجود بیرون web root را ترجیح بده، بدون فرض اینکه parent همیشه خصوصی یا writable است.
- اگر ذخیره در web root لازم شد، فرمت ذخیره‌سازی‌ای انتخاب کن که با درخواست مستقیم نه داده خام را برگرداند و نه محتوای نمونه را اجرا کند. پسوند تصادفی یا `.bin` به‌تنهایی جلوی دانلود را نمی‌گیرد.
- برای نمونه‌ها از envelope غیرقابل‌اجرای امن، encoding و شناسه تصادفی استفاده کن؛ هرگز نمونه را `include`/`eval` نکن. در نبود محل امن، قابلیت mutation و ذخیره secrets باید fail closed باشد.
- مجوز فایل‌ها/پوشه‌ها، temporaryها و backupها را محدود کن. موفقیت ساخت protection را واقعاً بررسی کن.
- فایل‌های کنترل داخلی، config، knowledge و خود scanner از preview عمومی فایل، AI و delete عادی خارج باشند.

**پذیرش:** HTTP بدون login به تمام فایل‌های state/config/manifest/temp/quarantine اطلاعات حساس برنگرداند؛ نمونه قرنطینه تحت Apache، Nginx و PHP server اجرا نشود. اسکن/بازبینی با نصب تک‌فایل همچنان کار کند.

### [ ] F02 — رأی، اعتماد و حذف را به بایت‌های همان فایل متصل کن

**محل:** `src/Http.php:357,414,544,585,627`، `src/Hash.php`، `src/Gemini.php:53`، `src/Store.php:133,143`.

**مشکل:** auto-review checksum ذخیره‌شده هنگام scan را برای محتوای جدید استفاده می‌کند. `mark_clean` hash/path دلخواه کلاینت را می‌پذیرد. normalized hash بخش‌هایی از فایل را حذف می‌کند؛ این فرایند فقط یک نشانه تشخیص خانواده است و برابری محتوا را اثبات نمی‌کند.

**کار لازم:**

- هویت اعتماد و کش قابل‌استفاده برای تصمیم خودکار را **raw SHA-256** قرار بده. normalized hash فقط برای signature detection، گروه‌بندی احتمالی و نمایش شباهت باشد؛ حکم سالم/مخرب بین فایل‌های با raw hash متفاوت منتقل نشود.
- این تغییر، اصلاح ضروری تصمیم نسخه قبلی پلن است. رکوردهای trusted/cache مبتنی بر normalized hash بدون raw hash معتبر نباید خودکار به اعتماد جدید تبدیل شوند؛ آن‌ها را سابقه نیازمند بازبینی قرار بده.
- actionها `finding_id`، `scan_session_id` و expected raw hash بگیرند؛ سرور رکورد و مسیر را پیدا کند و hash فعلی را محاسبه کند. hash و path کلاینت مرجع حقیقت نباشند.
- قبل از AI، Mark legitimate و quarantine، regular-file بودن، root boundary، symlink، خوانده‌شدن و تغییر محتوا را بررسی کن. بعد از پاسخ AI نیز قبل از حذف hash را دوباره بررسی کن.
- در mismatch هیچ حذف یا اعتماد جدیدی اعمال نشود؛ `CHANGED_SINCE_SCAN` ثبت و preview/scan همان finding تازه شود.

**پذیرش:** فایل مخرب نمونه scan شود، سپس به فایل سالم تغییر کند؛ cache قبلی هرگز موجب حذف آن نشود. دو فایل با normalized برابر و raw متفاوت اعتماد/رأی را به ارث نبرند. hash/path ساختگی POST پذیرفته نشود.

### [ ] F03 — تراکنش قرنطینه و Restore را بدون overwrite و خروج از root انجام بده

**محل:** `src/Quarantine.php:3,47,96`، `src/Config.php:104`، `src/Http.php:402`.

**مشکل:** نام backup با تکرار همان path ثابت است؛ manifest بدون lock/check نوشته می‌شود. Restore وجود مقصد یا صحت checksum backup را بررسی نمی‌کند. canonicalization فقط `..` را حذف می‌کند و symlink والد را resolve نمی‌کند؛ backup بلافاصله بعد از copy حذف می‌شود.

**کار لازم:**

- برای هر رویداد quarantine شناسه یکتا بساز؛ backup قدیمی overwrite نشود.
- copy کامل، hash و ثبت موفق metadata باید قبل از unlink تأیید شوند؛ هر خطا فایل اصلی را نگه دارد و وضعیت واقعی را گزارش کند.
- Restore به مقصد موجود را رد کن و conflict واضح نمایش بده؛ انتخاب مقصد دیگر فقط با همان اعتبارسنجی امن.
- تمام والدهای موجود مقصد را با realpath/lstat بررسی کن؛ symlink در مبدأ backup یا زنجیره مقصد را نپذیر. ساخت parent نیز نباید بیرون root انجام شود.
- hash backup با manifest تطبیق یابد؛ در خرابی backup نه Restore انجام شود و نه نسخه پشتیبان حذف شود.
- بعد از Restore، status finding و گزارش به `RESTORED` تغییر کند و نسخه بازیابی‌شده قابل بازبینی باشد.

**پذیرش:** آزمون مسیر parent symlink، مقصد موجود، backup خراب، copy ناموفق، manifest ناموفق و قرنطینه چند نسخه از یک path، همگی بدون از دست رفتن فایل یا نوشتن خارج root تمام شوند.

### [ ] F04 — خروجی گزارش را در برابر داده کنترل‌شده توسط فایل مشکوک ایمن کن

**محل:** `src/Ui.php:233,523`، `src/assets/app.js`.

**مشکل:** JSON داخل `<script>` با `JSON_UNESCAPED_SLASHES` نوشته می‌شود. نام/مسیر فایل با markup می‌تواند script را ببندد. هم‌زمان API key کامل داخل textarea HTML قرار دارد؛ این ترکیب برای ابزار بررسی فایل آلوده خطر ویژه دارد.

**کار لازم:**

- داده embedded در script را با `JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT` و مدیریت UTF-8/خطای JSON encode کن؛ مسیر جایگزین امن هم قابل قبول است.
- داده فایل، rule، پاسخ AI و receipt فقط با textContent نمایش داده شود. HTML inline handlerهایی که داده متغیر دارند با event binding امن جایگزین شوند.
- API key ذخیره‌شده هیچ‌وقت در HTML/JS بازگردانده نشود؛ فقط وضعیت و چند کاراکتر mask‌شده نمایش بده. ورودی خالی یعنی حفظ کلید، و حذف/جایگزینی action جدا باشد.
- headerهای `Cache-Control: no-store` و `Referrer-Policy: no-referrer` برای صفحات حساس اضافه شوند.

**پذیرش:** مسیر فایل و پاسخ AI شامل `</script>`، quote، markup و UTF-8 نامعتبر UI را نشکند و script جدید نسازد. کلید ساختگی تست در HTML، JSON پاسخ یا گزارش یافت نشود.

## 5. P1 — تکمیل جریان اصلی و قابلیت اتکا

### [ ] F05 — ورود مجدد و setup را درست و قابل حفاظت کن

**محل:** `src/Http.php:26,53,102,212`.

**مشکل:** `authenticate()` کلید POST را معتبر تشخیص می‌دهد؛ در نتیجه handler login رد می‌شود و action `login` وارد router بدون case می‌شود. GET key نیز از همان مسیر عبور می‌کند و redirect حذف key اجرا نمی‌شود. setup عمومی فعلی هر نخستین مراجعه‌کننده را قادر به تنظیم کلید می‌کند؛ reset با GET و بدون CSRF است.

**کار لازم:** login/setup را قبل از router و با مسیر روشن پردازش کن؛ login صحیح session بسازد و redirect بدون کلید انجام دهد. برای راه‌اندازی اولیه اثبات دسترسی به فایل/هاست، مثلاً setup secret اختصاصی در فایل یا محیط، لازم باشد؛ کلید ثابتی در release نباشد. reset به POST+CSRF منتقل شود. محدودیت تلاش ورود و logout مشخص اضافه شود. اعتبار CSRF به session مشخص متصل باشد.

**پذیرش:** در cookie jar خالی login صحیح redirect بدهد، cookie بسازد و refresh گزارش را حفظ کند؛ login غلط mutation نکند. setup بدون setup secret و reset GET رد شوند. آزمون مهاجرت یا reset داده قدیمی هم state را ناخواسته پاک نکند.

### [ ] F06 — batch واقعی، حافظه محدود و پیمایش داخل root

**محل:** `src/Http.php:783,808`، `src/bootstrap.php`، `src/Store.php:133`.

**مشکل:** سقف زمان/تعداد فقط بین دایرکتوری‌ها بررسی می‌شود، `scandir` همه نام‌ها را بارگذاری می‌کند و محتوا بدون سقف خوانده می‌شود. symlink بیرون root دنبال می‌شود و cycle می‌تواند صف را بی‌پایان کند. knowledge برای هر فایل دوباره کامل parse می‌شود؛ CLI از process بازگشتی استفاده می‌کند.

**کار لازم:** cursor داخل دایرکتوری را persist کن و پس از هر فایل بودجه را بررسی کن؛ فقط افزودن `break` کافی نیست، چون بقیه پوشه گم می‌شود. پیمایش symlink را پیش‌فرض رد کن، realpath boundary و visited directory داشته باش. فایل بزرگ/ناخوانا با وضعیت coverage روشن ثبت شود. raw hash به‌صورت streaming و محتوای تحلیلی با سقف مشخص خوانده شود. knowledge یک‌بار در batch خوانده شود. CLI همان runner را در یک loop با `PHP_BINARY` و بدون recursion اجرا کند.

**پذیرش:** پوشه ۶۲۰ فایل در حداقل دو batch پردازش شود؛ پس از وقفه هیچ فایل گم/دوباره‌شماری نشود. cycle، symlink خارجی، فایل حجیم و permission denied منجر به hang یا پیام «همه چیز اسکن شد» نشوند.

### [ ] F07 — ذخیره state و هماهنگی درخواست‌ها را crash-safe کن

**محل:** `src/Store.php:20,40,87,332,377`، `src/Config.php:228`، manifest قرنطینه.

**مشکل:** خواندن بدون lock و نوشتن با truncate می‌تواند JSON خالی/ناقص تولید کند؛ سپس کد آن را بی‌صدا state جدید فرض می‌کند. lock صرفاً هنگام write از lost update snapshot قدیمی جلوگیری نمی‌کند. claim فاقد شناسه مالک و session/revision است؛ reset هنگام AI می‌تواند index را به فایل دیگری اشاره دهد.

**کار لازم:** lock file ثابت + read/validate/update + temp write/rename با کنترل نتیجه و recovery مشخص؛ برای claim از token، session ID و expiry استفاده کن و commit فقط با همان token پذیرفته شود. network call نباید lock عمومی را نگه دارد. reset هنگام job فعال یا ممنوع باشد یا generation جدیدی بسازد که پاسخ قدیمی نتواند آن را تغییر دهد. خطای JSON، دیسک و permission باید قابل مشاهده و بدون موفقیت کاذب باشد.

**پذیرش:** دو process هم‌زمان، قطع نوشتن، state خراب، دیسک غیرقابل‌نوشتن، پاسخ دیرهنگام AI و reset وسط کار داده را از بین نبرند. ذخیره تنظیم غیرمرتبط کلید محیطی را روی دیسک ننویسد؛ rename ناموفق نتیجه false بدهد.

### [ ] F08 — Gemini قابل استفاده و پاسخ آن قابل اعتبارسنجی باشد

**محل:** `src/Config.php:183`، `src/Gemini.php:57,234`، `src/Ui.php:531`.

**مشکل:** پیش‌فرض هنوز `gemini-2.0-flash` است. مستندات رسمی فعلی خاموش‌شدن آن در 2026-06-01 را اعلام می‌کند. `floatval` روی مقدار نامعتبر confidence ممکن است شرط quarantine را پاس کند؛ پاسخ ناقص، finish reason و اندازه خروجی سخت‌گیرانه بررسی نمی‌شوند. [Google model deprecations](https://ai.google.dev/gemini-api/docs/deprecations)

**کار لازم:** default و fallback را در یک محل تعریف کن، مدل فعال Flash قابل تنظیم باشد و دکمه تست اتصال با درخواست کوچک و بدون فایل سایت اضافه شود. هنگام release شناسه و پارامترهای مدل از اسناد رسمی بررسی شوند؛ retry حدسی برای هر 400 انجام نشود. response schema را سمت سرور نیز validate کن: enum معتبر، confidence عدد finite در بازه ۰ تا ۱، required fieldها و طول رشته‌ها. پاسخ blocked، truncated، parse error و field نامعتبر نتیجه `uncertain/error` داشته باشد.

**پذیرش:** تست واقعی opt-in با مدل انتخاب‌شده موفق شود؛ بدون کلید اسکن دستی کار کند. confidence مانند `90invalid`، `90`، null، array و فیلد ناقص هرگز تصمیم حذف نسازند. هیچ کلیدی در log/error response نیاید.

### [ ] F09 — رأی ناقص AI را به حکم حذف کل فایل تبدیل نکن

**محل:** `src/Gemini.php:5,36,57`، `src/Http.php:585`، `src/assets/app.js:333,468`.

**مشکل:** فایل بزرگ فقط با پنجره ۳۲KB بررسی می‌شود؛ `buildSnippet` در عمل بدون محل match فراخوانی می‌شود و ممکن است شواهد را حذف کند. redaction بعد از بریدن snippet می‌تواند نام متغیر را بیرون پنجره و مقدار secret را داخل آن بگذارد. prompt فقط یک جمله دارد؛ snippet نامطمئن مستقیماً داده ورودی است. confidence ۸۵٪ به‌تنهایی ضمانت ایمنی نیست.

**کار لازم:** full/partial coverage و وضعیت redaction ثبت شود؛ در نسخه ساده، فایل بزرگ یا payload ناقص فقط manual review بگیرد و خودکار حذف نشود. محتوای فایل داده غیرقابل‌اعتماد باشد و هیچ دستور داخل آن دنبال نشود؛ ابزار execution/function call فعال نشود. reasons و evidence از سرور بیایند. قبل از اولین ارسال، کاربر بداند کد فایل به Google می‌رود. UI رأی AI را مشورت، نه تأیید قطعی نمایش دهد. شرط خودکار: پاسخ معتبر + raw hash منطبق + coverage کافی + شواهد + قرنطینه امن؛ uncertainty یعنی skip.

**پذیرش:** فایل بزرگ با نشانه خارج پنجره، prompt injection در comment، مقدار حساس روی مرز پنجره و پاسخ نامطمئن باعث انتقال پنهان secret، اعتماد دائمی یا حذف خودکار نشوند. بازبینی دستی و پاسخ cache‌شده بدون درخواست دوباره قابل مشاهده باشد.

### [ ] F10 — trust و cache را نسخه‌دار و UI دو تأیید را کامل کن

**محل:** `src/Store.php:143,210`، `src/Gemini.php:53`، `src/Http.php:544,585`، `src/assets/app.js:38,155,280`، `src/Ui.php:197`.

**مشکل:** پشتیبانی دو تأیید در store وجود دارد، اما UI فقط status را عوض می‌کند و فایل‌های trusted را از صف حذف نمی‌کند؛ `TRUSTED` و `TRUSTED_HIDDEN` یکسان استفاده نمی‌شوند. rules version همیشه مقدار پیش‌فرض `rules_v1` می‌ماند و sync آن را عوض نمی‌کند. Ask AI دستی رأی را در session ذخیره نمی‌کند و بعد از refresh badge از بین می‌رود.

**کار لازم:** پس از F02 کلیدها را بر raw hash بنا کن. تأیید دوم فقط رویداد مستقل روی finding معتبر در مسیر دیگر یا scan بعدی باشد؛ double-click رویداد جدید نسازد. نمایش سابقه تأیید اول، حذف تمام duplicateها از review پس از دوم و Revoke قابل اتکا باشد. cache شامل raw hash، digest قواعد فعال، نسخه prompt/redaction و شناسه مدل مشخص باشد؛ اگر path/reasons در prompt مؤثرند، context fingerprint نیز لازم است. alias متغیر مدل نباید cache دائمی بدون سیاست refresh بسازد.

**پذیرش:** با سه فایل هم‌بایت، تأیید A و B موجب حذف C از صف جاری و اسکن بعدی شود؛ Skip هیچ رأیی ننویسد. تغییر rules/model/prompt cache قبلی را برای تصمیم جدید استفاده نکند. Ask AI دستی پس از reload سابقه معتبر داشته باشد.

### [ ] F11 — blind spotهای تشخیص و تعریف coverage را روشن و اصلاح کن

**محل:** `src/Engine.php:12,99`، انتخاب پسوند در `src/Http.php`، `rules/database.json`، README.

**مشکل:** نام `.l10n.php` و شروع `return [` مدرک رسمی یا سالم‌بودن فایل نیست، اما بیشتر detectorها را خاموش می‌کند. `uploads/index.php` صرفاً با نام از rule مسیر معاف است. substringهای عمومی مثل `fileToUpload` به‌تنهایی false positive می‌سازند. بعضی regexها فقط ۱MB اول را می‌بینند و گزارش این محدودیت را نشان نمی‌دهد.

**کار لازم:** whitelist براساس نام/prefix را حذف یا به بررسی ساختار واقعاً محدود و بدون execution تبدیل کن؛ exact verified checksum می‌تواند استثنا باشد. امضاهای پرنویز را «مشکوک» با rule ID و evidence بنام، نه ویروس قطعی. پسوندهای PHP رایج و گزینه deep scan فایل‌های متنی مرتبط را تعریف کن و غیرپشتیبانی‌ها را بشمار. دامنه v1 فایل‌های سایت باشد؛ بررسی دیتابیس به‌عنوان قابلیت موجود معرفی نشود.

**پذیرش:** فایل با نام ترجمه و محتوای اجرایی اضافی از content detection فرار نکند؛ index.php مشکوک بررسی شود. corpus سالم و مشکوک شامل موارد obfuscation و پسوندهای هدف آزمایش و تعداد FP/FN گزارش شود. هیچ عبارت «تمام سایت سالم است» صرفاً بر پایه نبود finding نمایش داده نشود.

### [ ] F12 — auto-review قابل pause/resume و گزارش آن ماندگار باشد

**محل:** `src/assets/app.js:438,477`، `src/Store.php:332`، `src/Http.php:585`، `src/Ui.php:464`.

**مشکل:** چند بار start/resume می‌تواند چند loop بسازد. نبود claim به‌اشتباه finished است حتی اگر کار دیگری `AI_PROCESSING` باشد. شمارنده‌ها فقط در JS هستند و ۱٫۵ ثانیه بعد از پایان با reload از بین می‌روند. خطای شبکه ممکن است loop بی‌نهایت بسازد؛ force_error به «فایل بعدی» وابسته است.

**کار لازم:** یک job ID، in-flight guard و وضعیت persisted `running/paused/completed/failed` داشته باش. Pause/Cancel جلوی کار بعدی را بگیرد و وضعیت درخواست درحال اجرا روشن باشد. تعداد pending، processing، skipped، cache hits، failures، quarantined و restored از سرور محاسبه شود. cooldown/backoff و retry محدود سمت سرور باشند؛ `Retry-After` رعایت شود. cooldown فعلی static است و بین HTTP requestها از بین می‌رود؛ این را persist کن.

**پذیرش:** دو تب، قطع اتصال، refresh، pause/resume، 429 و timeout باعث حذف تکراری، تمام‌شدن کاذب یا گم‌شدن گزارش نشوند. گزارش پایان بعد از reload و ورود مجدد باقی بماند. چند کلید یک Google project به‌عنوان افزایش خطی سهمیه معرفی نشوند؛ محدودیت‌ها project-level هستند. [Google rate limits](https://ai.google.dev/gemini-api/docs/rate-limits)

### [ ] F13 — قرارداد قواعد و دریافت update قابل اعتبارسنجی باشد

**محل:** `src/Rules.php:33,76,151`، `src/Engine.php`، `src/Http.php:467`، `bin/build.php`.

**مشکل:** `schema_version` فقط از نظر وجود بررسی می‌شود؛ type و required fieldها به‌قدر کافی validate نمی‌شوند. `normalized_hash` بدون pattern می‌تواند معتبر ولی بی‌اثر باشد؛ rule IDها در finding حفظ نمی‌شوند. sync از helper همیشه POST استفاده می‌کند، درحالی‌که دریافت فایل JSON به GET نیاز دارد. نوشتن override بررسی نمی‌شود و endpoint اولیه هنوز `OWNER/REPO` است.

**کار لازم:** برای هر section/type قرارداد کوچک و صریح تعریف کن؛ فایل نامعتبر کامل رد شود و قواعد معتبر قبلی باقی بماند. rule ID، evidence و digest قواعد فعال ثبت شوند. برای دریافت rules transport GET با HTTPS، محدودیت اندازه، host معتبر و بدون redirect ناامن استفاده کن؛ override ابتدا validate و سپس atomic replace شود. remote sync تا مشخص‌شدن منبع واقعی خاموش باشد. هیچ rule دریافتی کد PHP اجرا نکند.

**پذیرش:** JSON بدشکل، schema ناشناخته، pattern غلط/بزرگ، section اشتباه و HTTP error قواعد فعال را خراب نکنند؛ افزودن یک literal/regex معتبر بدون ویرایش موتور کار کند. sync موفق digest cache را تغییر دهد و endpoint تستی روش GET را ببیند.

### [ ] F14 — مشارکت فقط یافته‌های انتخاب‌شده و با preview واقعی ارسال کند

**محل:** `src/Share.php:3,69,141`، `src/Http.php:713`، UI اشتراک.

**مشکل:** bundle همه findingها از جمله trusted را تهدید معرفی می‌کند. include_samples همه manifest قرنطینه را، حتی خارج از گزارش فعلی، ارسال می‌کند. rule IDs موجود نیستند. حذف prefix مسیر یا چند password الگو تضمین anonymization نیست؛ یک خط می‌تواند کل source یا secret طولانی باشد.

**کار لازم:** فهرست انتخاب فایل، verdict/source تصمیم، مقصد، حجم و دقیقاً payload ارسالی در preview دیده شود؛ هیچ ارسال خودکاری در پایان scan نباشد. پیش‌فرض metadata فقط؛ sampleها opt-in و محدود به selection جاری با raw hash معتبر. محدودیت مجموع bytes/count، validation مسیر backup و redaction/حذف evidence نامطمئن اعمال شود. `OWNER/REPO` لینک فعال نسازد؛ نبود مقصد با دانلود محلی توضیح داده شود. پاسخ واقعی دریافت ذخیره شود، نه صرفاً toast موفقیت.

**پذیرش:** trusted/benign و quarantine خارج selection ارسال نشوند؛ بدون consent تماس شبکه صفر باشد. payload preview با ارسال نهایی یکسان باشد و raw sample فقط با انتخاب صریح وارد شود. seedهای ساختگی API key، domain و credential در metadata عمومی نشت نکنند.

## 6. P2 — تکمیل انتشار

### [ ] F15 — حذف رفتار خارج از scope و تکمیل UX/i18n

**محل:** `src/Http.php:736`، `src/assets/app.js:767`، `src/Ui.php`، ترجمه‌ها.

- self-destruct برخلاف پلن نهایی همچنان در backend و UI هست؛ آن را حذف کن. پاک‌کردن اسکریپت یا quarantine جزو Reset نباشد. حذف همه findingها دوباره اضافه نشود.
- متن‌های hard-code خطا، Settings، AI و rate limit به dictionary منتقل شوند؛ خطاهای AJAX پس از انقضای session JSON روشن برگردانند، نه HTML login.
- در preview، درخواست قبلی هنگام Next/Previous لغو یا با finding ID تطبیق داده شود؛ محتوای فایل A نباید زیر عنوان B نمایش داده شود. هنگام loading دکمه‌های تصمیم و کلیدهای تکراری کنترل شوند.
- focus trap، focus return، کلیدهای modifier، دکمه‌های disabled، چیدمان باریک و EN/FA واقعی آزمایش شوند. زبان در مسیرهای زیرپوشه cookie ناسازگار نسازد.

**پذیرش:** keyboard/mobile review بدون جابه‌جایی اشتباه محتوا و تصمیم کار کند؛ endpoint self-destruct وجود نداشته باشد؛ صفحات EN و FA پیام untranslated مهم نداشته باشند.

### [ ] F16 — تست‌های واقعی جریان و release gate

**محل:** `tests/run.php`، `bin/build.php`، خروجی‌های PHP.

- آزمون‌های F01 تا F15 را به suite اضافه کن، به‌ویژه login عادی و auto-review واقعی endpoint؛ شرط production را در تست دوباره ننویس.
- concurrency با دو process و barriers واقعی تست شود. renderer با path/AI output مخرب داده‌ای و UTF-8 نامعتبر بررسی شود.
- CI روی PHP 7.4، یک PHP 8 متداول و آخرین نسخه مورد ادعای پشتیبانی اجرا شود؛ `php -l` روی 8.5 سازگاری 7.4 را ثابت نمی‌کند.
- copy فقط artifact به پوشه خالی و تست setup → scan → login مجدد → review → quarantine → restore → rescan → گزارش ضروری باشد.
- builder ابتدا خروجی temp بسازد، validate/lint کند و سپس artifact سالم قبلی را جایگزین کند. دو build باید یکسان باشند؛ خطای write/lint نسخه قبلی را خراب نکند.

**پذیرش:** suite اصلی و regressionها، smoke تک‌فایلی، دو وب‌سرور هدف و matrix PHP موفق باشند. تست واقعی Gemini opt-in جدا گزارش شود؛ CI بدون کلید کار کند.

### [ ] F17 — README و readiness را مطابق رفتار اثبات‌شده بنویس

**محل:** `README.md`، `malware-cleaner.md`، `.gitignore`، metadata انتشار.

- عبارت‌های «zero timeout»، «پاک‌سازی زیر یک دقیقه»، «حذف false positive» و «۳ کلید = ۴۵ درخواست» را با حدود واقعی جایگزین کن.
- installation URL و repository واقعی را پیش از انتشار تعیین کن؛ به مقصد ساختگی اشاره نشود.
- یک فایل نصب در برابر فایل‌های runtime، فضای writable، نیاز اینترنت/HTTPS برای AI، coverage پسوندها، عدم اسکن دیتابیس و محدودیت partial scan توضیح داده شوند.
- امنیت quarantine، روش بازیابی، خطای permission، ریسک false positive/negative و نگهداری secrets مطابق پیاده‌سازی نهایی باشند.
- تعداد تست‌ها به‌روز و قوانین عدم commit کلید، config، state، نمونه quarantine و temporaryها کامل شوند. صرف گرفتن LICENSE و badges دلیل آمادگی امنیتی نیست.

**پذیرش:** هر ادعای README به یک رفتار آزموده‌شده اشاره کند. فایل release بدون نام شرکت قبلی، کلید خصوصی و link placeholder باشد.

## 7. ترتیب پیشنهادی و معیار پایان

ترتیب اجرا: **F01 → F02 → F03 → F04 → F05 → F07 → F06 → F08 → F09 → F10 → F12 → F11 → F13 → F14 → F15 → F16 → F17**.

F16 را از آغاز برای افزودن regression هر اصلاح استفاده کن؛ matrix نهایی در پایان اجرا شود. کلاس، framework، provider یا قابلیت جانبی جدید صرفاً برای افزایش پیچیدگی اضافه نشود.

نسخه زمانی آماده تحویل پایدار است که:

- [ ] تمام P0 و P1ها بسته و آزمون پذیرش آن‌ها ثبت شده باشد.
- [ ] با فقط artifact، اسکن محدود و ادامه‌پذیر و login مجدد کار کند.
- [ ] هر تصمیم به raw hash همان نسخه فایل متصل و فایل تغییرکرده از حذف محافظت شود.
- [ ] فایل قرنطینه از طریق HTTP نه اجرا و نه افشا شود؛ Restore خارج root یا روی فایل موجود ننویسد.
- [ ] benign/uncertain/error موجب حذف خودکار یا اعتماد دائمی نشوند.
- [ ] دو تأیید انسانی روی checksum یکسان واقعاً صف و اسکن بعدی را اصلاح کند.
- [ ] AI با مدل فعال آزموده شده و حالت بدون AI نیز کامل قابل استفاده باشد.
- [ ] گزارش نهایی پس از refresh باقی بماند و فایل بررسی‌نشده را «سالم» نشمارد.
- [ ] اشتراک فقط با preview/selection/consent انجام شود.

## 8. مبنای نمره

| بخش | نمره از ۴ | دلیل |
|---|---:|---|
| بسته‌بندی و ساختار توسعه | 4 | build تکرارپذیر و artifact مستقل عملاً تأیید شد |
| جریان محصول و UX | 2 | بیشتر اجزا موجودند؛ login مجدد، صف trusted و گزارش نهایی ناقص‌اند |
| تشخیص و تصمیم AI | 2 | قواعد و cache وجود دارند؛ blind spot، normalized identity و اعتبارسنجی تصمیم مشکل دارند |
| پایداری و کیفیت آزمون | 1 | ۲۲ تست سبز، اما سقف batch، overwrite state و پوشش integration کافی نیست |
| امنیت عملیات | 1 | root prefix check، TLS و CSRF وجود دارند؛ قرنطینه اجرایی، Restore و خروجی گزارش blocker دارند |
| **مجموع** | **10 / 20** | **پایه مفید و قابل تکمیل؛ هنوز آماده پاک‌سازی خودکار محیط واقعی نیست** |

این نمره درباره نسخه فعلی است. مسیر رسیدن به هدف اصلی روشن است و نیازمند بازنویسی کل پروژه نیست؛ اصلاح حدود اعتماد، عملیات فایل، جریان ورود و آزمون‌های انتهابه‌انتها اولویت دارد.
